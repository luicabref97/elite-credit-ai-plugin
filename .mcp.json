{
  "mcpServers": {
    "elite-credit-api": {
      "type": "http",
      "url": "${ELITE_CREDIT_API_URL}/mcp",
      "headers": {
        "Authorization": "Bearer ${ELITE_CREDIT_API_KEY}"
      }
    }
  }
}
